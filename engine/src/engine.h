#if !defined(ENGINE_H)
#define ENGINE_H

#include <easylogger/easylogger.h>

// These definitions are replicated in logger.h
easylogger::Logger OPENGL("OPENGL");
easylogger::Logger GLEW("GLEW");
easylogger::Logger GLFW("GLFW");
easylogger::Logger LIBPNG("LIBPNG");

#endif